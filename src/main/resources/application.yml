spring:
  datasource:
    url: jdbc:postgresql://host.docker.internal:5432/postgres
    username: postgres
    password: password
    pool-size: 30
---
server:
  error:
    include-message: always
  port: 8080
